Tweet Reader:
    Runs in while True (possible multithreading?
    Adapt tweet reader so that it checks a new read against a list of IDs that have read so far
    distribute the tweets into the relevant match objects

class Match
    process list of reported scores to output the most appropriate

output variable
    create appropriate variable that will feed into html

html
    edit html to allow for differentiation between prem/1stN/1stS

web interface django/Flask
    decide which will be used and implement it (archive the other)
    run on multithread??
